# Mermaid Highlighting Examples

`all-diagram-types.mmd` contains one compact sample for every diagram type
recognized by this grammar. It is intended for syntax highlighting checks, not
as a rendering conformance suite.

The samples intentionally include common highlighting targets such as comments,
directives, labels, quoted strings, numbers, styles, subgraphs, HTML line break
tags, and diagram-specific keywords.
